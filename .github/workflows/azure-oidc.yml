name: AzureOidc

on:
  workflow_dispatch:

jobs:
  azure:
    name: Azure Oidc
    runs-on: ubuntu-latest
    steps:
    - name: Do some stuff
      run: |
        echo "Do some stuff $(date +%T)"
        sleep 5
        echo "Done some stuff $(date +%T)"